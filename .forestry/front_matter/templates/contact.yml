---
label: contact
hide_body: false
fields:
- name: blocks
  type: blocks
  label: Blocks
  template_types:
  - form
  config:
    min: 
    max: 
- name: layout
  type: text
  config:
    required: false
  label: Layout
  default: page
  hidden: true
pages:
- contact.md
