---
label: locations
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: locations
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: location
  label: Locations
- name: layout
  type: text
  config:
    required: true
  label: Layout
  default: locations
