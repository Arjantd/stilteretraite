backend:
  name: git-gateway
  branch: master
collections:
  - name: "posts"
    label: "Posts"
    folder: "_posts"
    create: true
    slug: "/posts/{{slug}}/"
    fields:
      - {label: "Title", name: "title", widget: "string"}